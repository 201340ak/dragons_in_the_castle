// Native platform packages may be added when iOS/Android packaging is requested.
const config = {
  appId: 'com.dragonsinthecastle.game',
  appName: 'Dragons in the Castle',
  webDir: 'dist-mobile',
};

export default config;
